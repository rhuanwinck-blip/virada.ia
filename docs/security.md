# Segurança

Implementado/preparado:

- validação server-side com Zod;
- CSP e headers de segurança;
- webhook assinado fora do demo;
- idempotência prevista em pagamentos;
- RLS no schema;
- logs de auditoria;
- ambiente demo separado;
- `.env` fora do Git.

Nunca expor chaves no frontend ou liberar relatório por retorno do navegador.
