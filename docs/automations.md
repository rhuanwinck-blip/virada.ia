# Automações

Eventos principais ficam documentados em `lib/events.ts`.

n8n deve ser usado só para automações não críticas. Pagamento, liberação de acesso, scoring e autenticação ficam no backend.

Workflow exportável inicial: `automation/n8n/diagnostic-abandoned.json`.
